---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProcedureCard from '../../components/ProcedureCard.astro';
import { getCollection } from 'astro:content';

const procedures = await getCollection('procedimientos-mamarios');
const sorted = procedures.sort((a, b) => (a.data.order ?? 0) - (b.data.order ?? 0));
---

<BaseLayout
  title="Cirugías Mamarias"
  description="Aumento mamario, reducción y levantamiento de senos realizados por cirujanos plásticos especializados en Clínica Bendov, Las Condes."
>
  <section class="relative overflow-hidden bg-primary-950 pt-32 pb-20 md:pt-40 md:pb-28">
    <img src="/images/site/cat-mamarias.webp" alt="" class="absolute inset-0 h-full w-full object-cover opacity-40" loading="eager" decoding="async" />
    <div class="absolute inset-0 bg-gradient-to-br from-primary-950/60 via-primary-900/45 to-primary-800/30" />
    <div class="relative mx-auto max-w-4xl px-6 text-center">
      <p class="mb-4 text-sm font-semibold uppercase tracking-[0.15em] text-primary-300">Bendov Clínica Estética</p>
      <h1 class="font-display text-3xl font-bold text-white md:text-5xl">Cirugías Mamarias</h1>
      <p class="mt-4 text-lg text-primary-200 md:text-xl">
        Aumento, reducción y levantamiento mamario realizados por cirujanos plásticos especializados con más de 25 años de experiencia.
      </p>
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-6 py-16 md:py-24">
    <div class="grid grid-cols-2 gap-4 sm:gap-6 lg:grid-cols-3">
      {sorted.map((proc) => (
        <ProcedureCard
          title={proc.data.title}
          href={`/procedimientos-mamarios/${proc.data.slug}/`}
          description={proc.data.description}
          image={proc.data.thumbnailImage}
          buttonLabel="Ver Procedimiento"
        />
      ))}
    </div>
  </section>
</BaseLayout>
