---
import BaseLayout from '../layouts/BaseLayout.astro';
import WhatsAppIcon from '../components/icons/WhatsAppIcon.astro';
import { siteConfig } from '../data/site';

const videos = [
  {
    id: 'bcbwTQuwVVU',
    title: 'Experiencias de pacientes en Bendov Clínica Estética',
  },
  {
    id: 'rQvux6vhFn0',
    title: 'Experiencias de pacientes en Bendov Clínica Estética',
  },
];
---

<BaseLayout
  title="Experiencias de Pacientes"
  description="Conoce las experiencias reales de nuestros pacientes en Bendov Clínica Estética. Testimonios y resultados que hablan por sí solos."
  noindex={true}
>
  <!-- Hero -->
  <section class="relative overflow-hidden bg-primary-950 pt-32 pb-20 md:pt-40 md:pb-28">
    <div class="absolute inset-0 bg-gradient-to-br from-primary-950/90 via-primary-900/80 to-primary-800/70" />
    <div class="relative mx-auto max-w-4xl px-6 text-center">
      <p class="mb-4 text-sm font-semibold uppercase tracking-[0.15em] text-primary-300">Bendov Clínica Estética</p>
      <h1 class="font-display text-3xl font-bold text-white md:text-5xl lg:text-6xl">Experiencias de Pacientes</h1>
      <p class="mx-auto mt-6 max-w-2xl text-lg text-primary-200 md:text-xl">
        Nada habla mejor de nuestro trabajo que las experiencias reales de quienes han confiado en nosotros.
      </p>
    </div>
  </section>

  <!-- Intro -->
  <section class="mx-auto max-w-4xl px-6 py-16 md:py-20 text-center">
    <p class="text-sm font-semibold uppercase tracking-[0.15em] text-primary-600">Testimonios Reales</p>
    <h2 class="mt-2 font-display text-3xl font-bold text-neutral-950 md:text-4xl">
      Lo que dicen nuestros pacientes
    </h2>
    <p class="mx-auto mt-4 max-w-2xl text-lg leading-relaxed text-neutral-500">
      En {siteConfig.name} nos enorgullece la confianza que nuestros pacientes depositan en nuestro equipo.
      Cada historia es un testimonio de compromiso, profesionalismo y resultados que transforman vidas.
    </p>
  </section>

  <!-- Videos -->
  <section class="mx-auto max-w-6xl px-6 pb-20 md:pb-28">
    <div class="grid grid-cols-1 gap-10 lg:grid-cols-2">
      {videos.map((video) => (
        <div class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-neutral-100">
          <div class="relative aspect-video">
            <iframe
              src={`https://www.youtube-nocookie.com/embed/${video.id}`}
              title={video.title}
              loading="lazy"
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
              allowfullscreen
              class="absolute inset-0 h-full w-full"
            />
          </div>
          <div class="p-5">
            <h3 class="font-display text-lg font-semibold text-neutral-900">{video.title}</h3>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- Additional Testimonials Text -->
  <section class="bg-neutral-50 py-20 md:py-28">
    <div class="mx-auto max-w-6xl px-6">
      <div class="text-center">
        <h2 class="font-display text-3xl font-bold text-neutral-950 md:text-4xl">
          Miles de pacientes satisfechos
        </h2>
        <p class="mx-auto mt-4 max-w-2xl text-lg text-neutral-500">
          A lo largo de los años, hemos ayudado a miles de pacientes a alcanzar sus objetivos estéticos con seguridad y profesionalismo.
        </p>
      </div>

      <div class="mt-14 grid grid-cols-1 gap-8 sm:grid-cols-3">
        <div class="rounded-2xl bg-white p-8 text-center shadow-sm ring-1 ring-neutral-100">
          <div class="font-display text-4xl font-bold text-primary-700">25+</div>
          <p class="mt-2 text-neutral-500">Años de experiencia</p>
        </div>
        <div class="rounded-2xl bg-white p-8 text-center shadow-sm ring-1 ring-neutral-100">
          <div class="font-display text-4xl font-bold text-primary-700">6</div>
           <p class="mt-2 text-neutral-500">Médicos especialistas</p>
        </div>
        <div class="rounded-2xl bg-white p-8 text-center shadow-sm ring-1 ring-neutral-100">
          <div class="font-display text-4xl font-bold text-primary-700">35+</div>
          <p class="mt-2 text-neutral-500">Procedimientos disponibles</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="bg-primary-700 py-20">
    <div class="mx-auto max-w-4xl px-6 text-center">
      <h2 class="font-display text-3xl font-bold text-white md:text-4xl">
        Vive tu propia experiencia
      </h2>
      <p class="mt-4 text-lg text-primary-100">
        Agenda tu evaluación gratuita y descubre cómo podemos ayudarte a alcanzar tus objetivos.
      </p>
      <a
        href={siteConfig.contact.whatsappHref}
        target="_blank"
        rel="noopener noreferrer"
        class="mt-8 inline-flex items-center gap-2 rounded-full bg-white px-8 py-4 text-base font-semibold text-primary-700 shadow-lg transition-all hover:scale-105 hover:shadow-xl"
      >
        <WhatsAppIcon class="h-5 w-5 text-[#25d366]" />
        Agenda tu Evaluación Gratuita
      </a>
    </div>
  </section>
</BaseLayout>
